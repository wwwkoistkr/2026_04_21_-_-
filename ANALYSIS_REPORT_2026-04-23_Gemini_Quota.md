# 🔍 분석 보고서 — "지금 발송" 워크플로우 장기 실행 & Gemini 요약 실패

**작성일**: 2026-04-23 (KST 17:00경)  
**분석 대상**: Workflow Run #39 (Legacy Daily Briefing, 2026-04-23 07:32 UTC 시작)  
**분석자**: AI Developer  
**결론 한 줄**: **Gemini API 무료 티어 일일 할당량(일 20회)이 완전히 소진되어 10/10 기사 요약이 모두 폴백(원본 제목만 표시)으로 처리됨.** 코드 버그 아님 — v2.6.2는 정상 작동 중.

---

## 1. 📸 사용자 증상 요약

| 증상 | 내용 |
|------|------|
| **워크플로우 장시간 실행** | "지금 발송" 버튼 클릭 후 230초 경과 표시, 긴 시간 동안 멈춘 듯 보임 |
| **이메일 내용 빈약** | AI 요약이 거의 없고 원본 뉴스 제목만 나오는 패턴 반복 |
| **UI 표시** | 대시보드에는 Collect/Summarize/Send 모두 OK로 표시됨 (역설적) |

---

## 2. 🧪 실측 데이터 (Run #39 로그 분석)

### 2-1. 워크플로우 실행 시간
```
Run #39: Legacy Daily Briefing
  시작: 2026-04-23 07:32:12 UTC (16:32 KST)
  종료: 2026-04-23 07:37:52 UTC (16:37 KST)
  총 실행: 340초 (5분 40초)
```

**→ "230초 경과"는 중간 폴링 시점이었고, 실제 완료는 340초 소요.**  
**→ 사용자가 본 "너무 긴 워크플로우" 증상의 정체: Gemini 429 대기 루프.**

### 2-2. Gemini API 호출 통계 (결정적 증거)

| 항목 | 수치 |
|------|------|
| **총 API 호출 횟수** | **78회** |
| **성공 (200 OK)** | **6회** (7.7%) |
| **429 Quota Exceeded** | **70회** (89.7%) ← 🚨 **범인** |
| **503 Service Unavailable** | 1회 (1.3%) |
| **empty response** | 1회 (1.3%) |

### 2-3. 할당량 소진 상세 (로그 원문)
```
Quota exceeded for metric: 
  generativelanguage.googleapis.com/generate_content_free_tier_requests
  limit: 20, model: gemini-2.5-flash
  quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier
  quotaValue: '20'    ← 일일 최대 20회
```

**→ Gemini 2.5 Flash 무료 티어: 하루 20회 제한 / 프로젝트 / 모델.**  
**→ 오늘(04-23) 이미 20회 사용 완료 → 리셋까지 대기 필요.**

### 2-4. Fallback 모델 상태
| 모델 | 상태 |
|------|------|
| `gemini-2.5-flash` | ❌ 20회 일일 한도 초과 |
| `gemini-2.5-flash-lite` | ❌ 20회 일일 한도 초과 |
| `gemini-2.0-flash` | ❌ **limit: 0** (계정이 아예 접근 불가) |
| `gemini-1.5-flash` | ⚠️ 시도 안됨 (앞에서 실패하면 다음 모델 → 루프 종료) |

### 2-5. 처리 결과 (각 기사별 최종 상태)
```
Step 1) 랭킹 시도 → 4회 재시도 끝에 "모든 모델 실패" → 상위 10개 자동 선정 (폴백)
Step 1) v2.6.2 쿼터 강제 ✅ 적용: 미국 4건 + 한국 6건 = 10건 정상
Step 2) item 1 ~ item 10 → 전부 "전체 실패 → 원본 폴백"
Step 3) 총평(overview) → 기록 없음 (모두 폴백)
```

**→ 본문의 AI 요약은 0건, 10건 모두 원본 제목만 표시되어 이메일이 매우 빈약해 보였음.**

---

## 3. 🎯 근본 원인 분석

### 🥇 1순위: Gemini 무료 할당량 일일 한도 소진 (확정)

**오늘 API 호출 이력**:
| 시각 (KST) | 작업 | 호출 수 (추정) |
|------------|------|--------------|
| 06:00 | 자동 Collect (스케줄) | - |
| 06:00–06:10 | 자동 Summarize (1차) | ~13회 (ranking 1 + items 10 + overview 1 + 재시도) |
| 06:10–06:25 | 자동 Send | - |
| 13:12 | 수동 Legacy Briefing | ~13회 |
| 15:00–15:13 | 사용자 수동 "지금 발송" 테스트 여러 번 | ~40회 |
| 15:35 이후 | 방금 테스트한 Run #39 | **78회 시도 (대부분 429)** |

**계정 일 할당량 20회는 오전 중에 이미 소진됨.**

### 🥈 2순위: OpenAI 백업 경로 미구성
```
OPENAI_API_KEY: (빈 문자열)
OPENAI_BASE_URL: (빈 문자열)
OPENAI_MODEL:    (빈 문자열)
```
**→ Gemini 실패 시 OpenAI 호환 API로 폴백할 수 있으나 Secret이 설정되지 않아 사용 불가.**

### 🥉 3순위: 재시도 로직이 과도한 API 호출 발생
- **현재**: 한 번 호출 실패마다 `gemini-2.5-flash` → `flash-lite` → `2.0-flash` → `1.5-flash` 순회하며 각각 4회 재시도 = **최대 16회/1건**
- 10개 기사 × 16회 = **최대 160회 시도 가능** (할당량 20회 앞에서는 무의미한 낭비)
- **빠른 실패(fail-fast)** 로직이 없어서 4분간 "보이지 않는 대기" 발생

---

## 4. ✅ 성공한 부분 (긍정)

| 단계 | 결과 |
|------|------|
| **Collect** (뉴스 수집) | ✅ 한국 18건 + 미국 18건 = 36건 성공 |
| **Article Scraping** | ✅ 평균 1,500~2,500자 정상 추출 |
| **v2.6.1 Google News RSS** | ✅ 미국 6개 매체 전부 3건씩 수집 |
| **v2.6.2 쿼터 강제** | ✅ "미국 4건 + 한국 6건 = 10건" 로그 확인 |
| **v2.6.2 배포 상태** | ✅ 코드 정상 작동 (commit 3d59686) |
| **이메일 발송** | ✅ 3명 수신자에게 정상 전달 |

**→ 오늘 아침 v2.6.1 배포 당시 이미 미국 뉴스 수집이 복구되었고, v2.6.2는 섹션 분리도 정상 작동 중. 단지 AI 요약이 할당량 문제로 막힌 것.**

---

## 5. 🔧 해결 방안 (우선순위별)

### Option 1 — 🕛 대기 (즉시 비용 0원, 추천 ⭐⭐⭐)
- **Gemini 무료 할당량은 PT 자정 = 한국 오후 4~5시경 리셋** (공식 문서 기준).
- **현재 시각 17:00 KST 이후에는 이미 리셋되었을 가능성 높음.**
- **행동**: 지금 "지금 발송" 다시 1회 테스트 → 성공하면 코드는 정상 확정.

### Option 2 — 💰 Gemini 유료 플랜 업그레이드 (영구 해결, 추천 ⭐⭐⭐)
- **Gemini API Pay-As-You-Go**: https://ai.google.dev/pricing
- `gemini-2.5-flash` 가격: **입력 $0.30/M tokens, 출력 $2.50/M tokens**
- **예상 월 비용**: 일 12건 AI 호출 × 30일 × ~5K token = **월 약 $0.50~2 수준**
- 소진 걱정 없음, 매일 06:00 자동 발송 안정화.

### Option 3 — 🔑 OpenAI 백업 키 등록 (무중단 이중화, 추천 ⭐⭐)
```bash
# Cloudflare Pages Secret (또는 GitHub Actions Secret)에 추가
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1   # 또는 다른 호환 endpoint
OPENAI_MODEL=gpt-4o-mini                    # $0.15/M input, $0.60/M output
```
- Gemini 실패 시 자동으로 OpenAI로 전환되는 코드가 **이미 구현되어 있음** (ai_summarizer.py Line 844~).
- `gpt-4o-mini`는 Gemini보다 더 저렴.

### Option 4 — ⏱️ 호출 페이싱 조정 (코드 수정, 추천 ⭐)
- 현재 `SUMMARY_CALL_DELAY_SEC=6` → 10 RPM 안전 범위
- **하지만 RPM이 아니라 일일(RPD) 한도가 문제**이므로 페이싱 조정으론 해결 안됨.
- `GEMINI_RETRY_DELAYS_SEC = (2, 4, 8)`을 `(0,)`로 단축 → 429 인지 시 즉시 다음 모델로 점프 (시간 단축만 효과).

### Option 5 — 📱 429 감지 시 UI 피드백 (UX 개선, 추천 ⭐)
- 현재 UI는 "진행 중… 230초 경과"만 표시 → 사용자 혼란
- "⚠️ Gemini 일일 할당량 소진 — 내일 06:00 KST 자동 재개" 안내 메시지 추가.
- GitHub Actions 로그에 `Quota exceeded` 발견 시 run-progress KV에 `status: 'quota_exceeded'` 기록.

---

## 6. 💡 전문가 권고

### 즉시 조치 (지금 ~ 오늘 밤)
1. **지금 바로 "지금 발송" 테스트 1회** → PT 자정이 지났으면 정상 동작 확인 (예상 성공).
2. 실패하면 **04-24 06:00 KST까지 대기** → 자동 실행으로 리셋 후 동작 확인.

### 단기 조치 (48시간 내)
3. **Option 3 — OpenAI 백업 키 등록** (5분, 보험 성격).
   - `sk-...` 키만 Cloudflare Secret에 추가하면 됨.
   - 코드 수정 불필요 (이미 구현).

### 중기 조치 (1주일 내)
4. **Option 2 — Gemini 유료 플랜 업그레이드** (Google Cloud Billing 등록).
   - 월 $1~2 수준 예상, 안정성 확보.
   - OR Option 3로 커버 가능.

5. **Option 5 — UI에 할당량 에러 표시** (30분, v2.6.3).

---

## 7. 🚫 **하지 말아야 할 것**

| ❌ 잘못된 대응 | 이유 |
|--------------|------|
| 코드를 또 수정한다 | 이미 v2.6.2 정상. 원인은 코드 바깥(API 할당량). |
| 오늘 계속 "지금 발송"을 누른다 | 할당량 소진 상태에서는 매번 똑같이 실패. 오히려 내일 리셋 시간 지연 가능성. |
| Gemini API Key를 바꾼다 | 할당량은 프로젝트 단위. 새 키 발급해도 같은 프로젝트면 효과 없음. |
| `main.py all`에서 폴백 로직을 제거한다 | 폴백 덕분에 이메일은 최소한 발송됨 (원본 제목으로). 제거하면 아예 실패. |

---

## 8. 📅 예상 복구 시점

| 조치 | 예상 시각 |
|------|----------|
| **자연 복구 (Gemini 할당량 리셋)** | 2026-04-23 16:00~17:00 KST (PT 자정) 또는 04-24 00:00 KST 중 가까운 시점 |
| **내일 06:00 KST 자동 발송** | 정상 작동 예상 (단, 오전 중 여러 번 수동 실행 자제 필요) |
| **OpenAI 백업 설정 시** | 즉시 무중단 사용 가능 |
| **Gemini 유료 전환 시** | 결제 승인 후 15분 이내 |

---

## 9. 🎭 요약: "문제 같지만 문제 아닌 것"

```
증상 레이어              실제 원인 레이어
────────────            ────────────────
이메일에 요약 없음   →   Gemini 10/10 호출 실패 (429)
워크플로우 너무 김    →   재시도 루프 4분 (16회/건 × 10건)
미국 뉴스 안 나옴    →   Collect는 정상, 요약 실패로 내용 부재
대시보드는 OK 표시   →   HTTP 200/Job Success만 체크 (API 내용 품질 X)
```

**→ 시스템 전체는 건강하지만, 외부 무료 API의 일일 한도에 걸린 상태.**

---

## 10. 🆘 추가 도와드릴 일

| 선택 | 작업 내용 | 소요 시간 |
|------|----------|---------|
| **A** | 지금 당장 "지금 발송" 1회 재테스트 → 할당량 리셋 여부 확인 | 5분 |
| **B** | OpenAI API Key 받아서 Cloudflare Secret 등록 도와드리기 | 10분 |
| **C** | v2.6.3로 429 감지 시 UI 에러 메시지 추가 + fail-fast | 45분 |
| **D** | Gemini 유료 전환 가이드 단계별 설명 | 10분 |
| **E** | 내일 아침 06:00 실행 결과까지 대기 후 재점검 | — |

**원하시는 옵션 번호를 알려주세요.** 예: "A 먼저 해보고, 실패하면 B"

---

*Generated by AI Developer | Run #39 ID: 24822851579 | Cloudflare Deployment: 7b015730*
